import LinerGradient from 'react-native-linear-gradient';
import styled from 'styled-components/native';

export default styled(LinerGradient).attrs({
  colors: ['#01bdbc', '#00D6D5'],
})`
  flex: 1;
`;
